> NOTE: the document header says 23 Aug; it was produced 19 Aug. Dates inside are otherwise consistent with boarding 23 Aug.

ummary": "Map the built stack against the final deliverable and synthesize ranked pathways to close the gap",
  "agentCount": 6,
  "logs": [
    "5/5 axes mapped, 38 gaps identified"
  ],
  "result": {
    "doc": "All anchors verified on disk. Writing the pathways document now.

---

# TRUSTFAKE WP1+WP4 â PATHWAYS DOCUMENT
## From the built stack to the 4 Sep deliverable
*Grounded 23 Aug 2026, ~22:20. All file paths under `/home/samuel-renteria/Desktop/FILES/PROJECTS/trustfake/wp1/` unless absolute. GPU status at writing: `runs/clip_attacks.sh` in flight (`vit_l14_e2e` pgd_linf running, overconf queued; 18.4/24.5 GB used).*

---

## 1. THE GROUND THESIS

In the deck's own words â "Confidence can be hackedâ¦ slipping fakes past human review" â this project can now say: **we measured that, priced it, and priced the fix.** A moderation policy tuned to a 5% residual-risk SLA on clean data holds at 4.65% on test; a one-grey-level perturbation that survives a saved PNG (`ace_uint8`, mean 0.261 grey levels) takes it to **33.06% wrong auto-decisions at bit-identical accuracy 0.8311**, with coverage *rising* to 50.4% â no accuracy monitor would fire (`runs/moderation_threatmodel.md`, `runs/condition_table_threatmodel.md`). Temperature calibration provably cannot help â asserted as `tests/test_metrics.py::test_temperature_cannot_change_binary_selective_metrics`, not cited. Adversarial training at the *forensic* epsilon 2/255 (8/255 collapses â `runs/train_wp3_eps8_255_collapsed.log`) restores the SLA: PGD residual risk 100%â13%, ACE 19.2%â8.05%, at the honestly-stated price of the review bill doubling 27%â50% (`runs/d4_*.json`, `runs/d3_comparative.md`). Every accuracy is read against the dataset's 0.9785 no-model shortcut floor (`runs/trivial_baselines.json`), the CLIP L/14 probe's 0.9291 is geometry-controlled, and the residual threat is named: the label-free over-confidence attack, the only one that drives coverage *up* â it does not make the system abstain, it stops it abstaining. That is "detectors that stay honest, robust under perturbation, calibrated under attack, safe for human-in-the-loop moderation" â with a number attached to each clause, all in `runs/`.

---

## 2. THE CRITICAL PATH

Three hardness classes govern sequencing: **NETWORK-HARD** (impossible after boarding), **GPU-HARD** (impossible after the 3090 is left behind â camp VRAM is ~9 GB shared, so ViT-L/14 attack work cannot slip), **PLANE-CAPABLE** (CPU, offline â slides freely). The PF dossier eats unknown hours today, so pre-boarding is ranked; **â = survives the cut in half**. Everything unstarred moves to the plane or camp without killing anything.

### PRE-BOARDING (tonight, 23 Aug â network + 3090)

| # | Item | What / files | Cost | Unblocks |
|---|---|---|---|---|
| P1 â | **ResNet weight fetch + offline acceptance test** | `hf download timm/resnet18.tv_in1k resnet50.tv_in1k resnet50.tv2_in1k resnet18.a1_in1k resnet50.a1_in1k` (~1.1 GB; `--include "*.safetensors" "*.json"` halves it) + torchvision trio into `~/.cache/torch/hub/checkpoints` (~240 MB, teammates' code path insurance). Then the exact check that failed: `HF_HUB_OFFLINE=1 CUDA_VISIBLE_DEVICES="" .venv/bin/python -c "import timm; [timm.create_model(m, pretrained=True) for m in [...]]"`. Verified tonight: no `resnet*` in `~/.cache/huggingface/hub`, no `~/.cache/torch/hub/checkpoints` at all. | 30 min attended, no GPU | D1 parity, all camp ResNet work. **Irreversible if missed.** |
| P2 â | **`ensurepip`** | `.venv/bin/python -m ensurepip --upgrade` â verified the uv venv has no pip; offline installs at camp are otherwise impossible. | 5 min | Any camp-side dependency fix |
| P3 â | **Sournac email, freeze-date line** | Add "Is code freeze Tue 1 Sep or Thu 3 Sep?" to the already-pending email (open item per current-state). Send tonight. | 5 min | Week-2 scheduling |
| P4 â | **GPU queue script, nohup'd behind `clip_attacks.sh`** | One bash script that waits for the running PID, then executes in this order: **(a)** ViT-only cells that can never run at camp â fgsm, pgd_l2, underconf on `vit_l14_e2e` `--sample 4000` (~1.5â2 h); **(b)** demo attacked-twin dump â `ace_uint8 --dump-images` over ~10 curated uids (seconds; needs the small `run_attacks.py` flag from P7); **(c)** cheap effb0 cells â fgsm Ã4, base-probe pgd_linf + overconf, pgd_l2 Ã4, underconf Ã4 (~1.5 h); **(d)** the AutoAttack triple on the three D3 arms via `attack_suite.autoattack_safe` (4.5â9 h, runs while he sleeps/packs; per-arm, so interruption loses only the tail). | 30 min attended to write; ~8â12 h unattended GPU | D2 completeness: fgsm/pgd_l2/underconf/autoattack cells, ViT column, the live-demo asset |
| P5 â | **Contract v1.1 â the sigma slot** (full spec in Â§3) | README interface block (lines 10â25) + `CONTRACT.md` extraction + `src/verify.py` sigma checks; **commit and push** â the one code item that wants network (camp wifi is unreliable, not absent, so a missed push is survivable but risky). | 2.5 h, no GPU | The entire sigma seam; Day-1 interface ownership |
| P6 | **Repo decision prep** (see Â§6.2) | If option 2 chosen: create empty `trustfake-harness` repo and push the seed **today** while network exists. Content refreshes from camp. | 1â2 h | Day-1 publication venue |
| P7 | **`--dump-images` flag** on `src/run_attacks.py`'s ace_uint8 path + CPU curation of flip uids from `predictions_effb0_standard_eps2_255_test_{clean,ace_uint8_eps0.005}.npz` + frozen thresholds in `runs/moderation_report.json` (t_low=0.0929, t_high=0.8881) | Small code + numpy; feeds P4(b) | 1 h CPU | The talk's demo moment + fallback recording |
| P8 | **`src/d2_matrix.py`** â the one-table D2 vulnerability matrix (modeled on `src/compare.py`, 97 lines; glob `runs/predictions/`, group via `attack_suite.parse_condition`, prediction rows robust-acc-only where `frac_perturbed<1`, confidence rows acc/AUROC(fail)/AURC/residual risk, manifest-sha + n_op + batch footer, `--` for missing cells). Testable now against the 44 existing npz while the GPU is busy. | 2â3 h CPU | D2 as a *deliverable*, and the file teammates' attack npz land in |
| P9 | **Screen-record the demo flip** once P4(b) lands (tampered â REVIEW; upload one-grey-level twin â ALLOW; reveal). 2â3 min clip. | 30 min | Venue-wifi-proof fallback for the talk's payoff |

If the PF dossier eats everything: P1âP5 are ~3.5 attended hours and preserve every downstream option. P6âP9 degrade gracefully to plane/camp.

### PLANE / DAY 1 (offline; camp GPU contended)

| Item | What / files | Cost | Unblocks |
|---|---|---|---|
| Plane: **`notes/talk_arc.md`** | The full beat structure (cold open â Detect/Stress/Harden/Act â demo â limitations â handoff â brick close), every number cited to its `runs/` file. Needs zero network. Wording of claims re-checked once the claim-audit lands. | 3 h | The 4 Sep spine; slides become table-regeneration |
| Plane: **`fit_thresholds_2d` + sigma scoring + fixtures + tests** (Â§3, steps 3â6) | Pure numpy over `runs/predictions/` | 6 h | D4 two-axis, the Day-1 worked example |
| Day 1 AM â: **ResNet parity rows** â grab the camp GPU before subgroups do | `src.features --model resnet18.tv_in1k` (fit/calib/test + jpeg_q50, downscale_0.5) â `src.probe` â `src.score` â `src.run_attacks` (ace_uint8, overconf) â `src.moderation`; repeat features/probe/score for resnet50.tv_in1k. Zero code changes â every entrypoint verified backbone-parametric. â¤30 min GPU, fits 9 GB. Frame the (expectedly lower) frozen-probe score as **the floor that motivates WP3's fine-tuned ResNet** â UniversalFakeDetect ordering, said on Day 1, not discovered by others later. | 1.5â2 h attended, 0.5 h GPU | D1 deck parity; kills "where is our baseline?" |
| Day 1 â: **Stem-collision guard** | 3-line assert in `src/probe.py _load()` and `src/models.py build_probe_detector()`: sidecar `meta['model_id']` must equal the requested full backbone id (both tags share stem `resnet18`; silent head/backbone mismatch otherwise). Canonical tag: **tv_in1k**. | 0.5â1 h CPU | Attributable D1 rows |
| Day 1 â: **Publish CONTRACT.md + the submission checklist**, demo the sigma fixture loop live ("here is what your MC-Dropout npz looks like; here is the table it prints") | Assembly, not authoring â the two 60-second warnings (AutoAttack-on-binary, normalization-inside-forward) are already written in README/models.py | 1 h | WP1 as the camp's scoreboard; Days 6â8 integration on our terms |
| Day 1: **`compare.py` graceful-skip** â warn-and-skip on missing `calib_clean` instead of `FileNotFoundError` (verified in source); widen the hardcoded column width / `effb0_` strip for foreign ids | 1 h CPU | First foreign checkpoint (~Day 4â5) scores without a crash |
| Day 1 evening: **one resnet18 standard arm** through `src.train --backbone resnet18.tv_in1k` (~10â20 min GPU), then ckpt: â run_attacks â score â moderation | Proves the WP3 handover lane on the architecture the PIs named | 0.5 h attended | D2/D3 scoring surface for WP3's models â *proven, not promised* |

### WEEK 1 (camp; CPU work parallel to subgroups' GPU use)

| Item | What / files | Cost | Unblocks |
|---|---|---|---|
| **`src/refresh.py`** â one-command matrix refresh: verify (hard gate) â score per model â D3 table â per-model D4 â **new cross-model D4 pivot** (the currently hand-assembled README table becomes generated) â figures. Acceptance: reproduces existing README numbers bit-for-bit from the 44 npz. | 3â4 h CPU | Absorb any new submission in minutes; hold the freeze |
| **`verify.py` stranger-hardening**: ground-truth cross-check of `y`/`label` against manifest columns (the shuffled-y poisoning hole), `np.isfinite(logits)`, bytes-dtype uid clean FAIL, "right rows WRONG ORDER" message via sorted-sha comparison + `tests/test_verify.py` fixtures | 3â4 h CPU | Trustworthy foreign npz; the D3/D4 tables in the talk |
| **Sigma week-1 items** (Â§3 steps 3â6 if not finished on the plane) + `moderation.py` two-axis | 3â5 h CPU | D4's deck-diagram moment |
| **README reorder** (thesis + three-number box + quickstart + contract + Detect/Stress/Harden/Act order + TOC) â **HARD dependency: after the claim-audit merges** | 2 h CPU | Stranger-readability on 4 Sep |
| **Brick packaging**: parameterize `verify.py` (`--pred-dir/--manifest`, pyarrow behind try-import), `pyproject.toml` (numpy+sklearn core; scipy/pyarrow optional extras), brick README, torch-dependent modules excluded | 4â5 h CPU | The TRAIL Factory commitment |
| **`notes/runbook_demo.md`** + fallback ladder (localhost â recording â screenshots) + presenting-machine asset checklist (venv 3.3 GB, timm caches, checkpoints 109 MB, moderation_report.json, manifest, sample-slice of cache256) | 2 h CPU | Demo survivability |
| *Conditional, only if WP3 subgroup asks for his budgets*: matched 3-arm resnet18 (~2.5 h GPU unattended) Â· *Optional*: eps sweep 1/255 & 4/255 on the D3 triple (~20 min GPU) + robust-acc-vs-eps panel | GPU, scheduled into idle slots | D3 depth |

### WEEK 2 (freeze week â assume freeze **Tue 1 Sep** until Sournac answers)

| Item | What / files | Cost |
|---|---|---|
| Demo sigma lane (Â§3 step 7) + fig3 policy-scatter regenerated with the first *real* sigma npz (swap file paths, one command) | 2 h + 1.5 h CPU |
| Slides from `talk_arc.md`: regenerate all tables via `src.score`/`src.compare`/`src.refresh` over the final npz set â teammates' models appear as columns, not rewrites; hand the deck's two-axis diagram slide to the sigma subgroup to present their own axis | 3â4 h |
| One full timed rehearsal (~24 min slot = 120 min / 5 projects) | 1 h |
| Anything code-shaped lands **before 1 Sep**; 2â3 Sep is buffer that may not exist |  |

**Conflict resolutions made here:** (1) baseline-parity vs d2-d3 GPU contention pre-boarding â resolved by hardness class: ViT cells and AutoAttack get tonight's 3090 (camp-infeasible or free-overnight); ResNet rows go to camp Day-1 8 AM (small VRAM, weights cached, camp-safe). (2) `talk_arc.md` moved from pre-boarding to the plane â it needs calm, not network, and P1âP5 need tonight. (3) The two competing sigma schemas across gap maps (`sigma`/`sigma_semantics` vs `unc`/`unc_kind`) are resolved to **one** canonical schema in Â§3 â the axis-5 `unc` naming is dropped.

---

## 3. THE SIGMA SEAM â full specification

**Boundary rule, stated once and enforced by the file format:** THEIRS = producing the sigma array (forward passes, ensemble members, scale choice, running it under attack). OURS = schema, validation, rank-based scoring, the fitted-and-frozen three-threshold policy, the degeneracy audit, the demo display. **The boundary is the npz.** This is how one person scores the PIs' own research area (uncertainty is Sournac's field) without building or debating it.

### 3.1 Contract v1.1 (pre-boarding, in README lines 10â25 + CONTRACT.md)

Canonical schema â this exact wording, no variants:

```
sigma   float32[n]   OPTIONAL â per-image uncertainty, higher = more uncertain
```
Mandatory sidecar block whenever `sigma` is present:
```json
"sigma_semantics": {
  "method":       "mc_dropout | deep_ensemble | evidential | ...",
  "n_samples":    <int>,                    // MC passes or ensemble members
  "scale":        "std_p_fake | predictive_entropy | variance | dirichlet_vacuity | ...",
  "higher_means": "more_uncertain",         // literal; producers convert, WP1 never flips
  "seed":         <int>
}
```
Contract rules stated in the document: sigmas of different `scale` are never compared in one table (same discipline as the batch-size check); Ï-thresholds are fitted per model on clean calib like everything else; **a submitted Ï must beat the MSP baseline *through the frozen policy* to count** â for Ï = f(MSP) the two-axis policy provably collapses to one-axis (the temperature-invariance corollary). That sentence is what makes the owner referee, not competitor.

### 3.2 Enforcement â `src/verify.py check()` (pre-boarding, ~line 99 after the y/logits dtype loop)

If `"sigma" in z`: require float32, shape `(n,)`, all finite, â¥0, `std > 0` (constant Ï = unwired head â FAIL), and a sidecar `sigma_semantics` with all five keys â FAIL otherwise. Also FAIL the orphan reverse (sidecar declares semantics, npz lacks the array). Existing consumers safe: verify tolerates unknown fields, all loaders index by key. WARN (not FAIL) when |Spearman(Ï, âMSP)| > 0.999.

### 3.3 Policy â `src/moderation.py` (plane/week 1, 3 h)

- `actions(p_fake, t_low, t_high, sigma=None, s_max=None)`: existing rule first, then `out[sigma > s_max] = REVIEW` applied **last** (high Ï overrides both ALLOW and FLAG â exactly the deck's diagram). `sigma=None` â bit-identical to today (asserted in tests).
- `fit_thresholds_2d(p_fake, sigma, y, sla_residual_risk=0.05, grid_p=100, grid_s=25) -> (t_low, t_high, s_max)`: for each `s_max` in `np.quantile(sigma, linspace)` **plus the mandatory +inf candidate**, mask `sigma <= s_max`, reuse the existing quantile double-loop on the masked subset with review rate over full n. The +inf candidate guarantees the 2-D fit never underperforms the shipped 1-D policy â a useless Ï yields `s_max = inf` and the report says so. ~25Ã today's fit cost: minutes of CPU on calib n=4,684. Grids recorded in the JSON.
- `evaluate_policy` grows two fields: `sigma_review_rate` and `sigma_review_unique` (items reviewed *only* because of Ï â the marginal value of the second axis, and the number the uncertainty subgroup is playing for).
- Frozen-policy doctrine transfers verbatim: fit on clean calib, freeze all **three** thresholds, apply to every condition; `s_max` + `sigma_semantics` written into `runs/moderation_report.json` (demo tolerates extra keys). Reuse the existing docstrings â do not rewrite the argument.

### 3.4 Scoring â `src/score.py` (1.5 h)

When the npz has Ï, emit a second row per condition with `axis='sigma'` using the already-data-agnostic primitives: `auroc_failure(-sigma, correct)`, `aurc(-sigma, correct)`, `augrc`, risk@cov via `operating_point_at_coverage(-sigma, â¦)`, n_op from `rc_curve` (negate: higher-uncertain = lower-confidence; direction guaranteed by the contract). Skip ECE/NLL/Brier for Ï rows â Ï is rank-only; print `-`, never a wrong number. **`metrics.py` core needs NO changes** â the hard-code is `conf = p.max(axis=1)` at `src/metrics.py:383` inside `summary()` (verified); either add an optional `conf=` override there ("keys are stable; add, never rename") or compute the Ï row from primitives in score.py directly. The tie-collapsed `rc_curve` is load-bearing: MC-Dropout Ï from T passes lives on a coarse grid with huge tie blocks; our permutation-invariance convention makes *their* numbers reproducible â a talking point, not plumbing.

### 3.5 Degeneracy guard (1 h, inside 3.4)

Ï = Spearman(Ï, 1âMSP) via numpy argsort ranks (scipy is already a dep via `fit_temperature`), printed on every Ï row. If |Ï| â¥ 0.995: print `DEGENERATE: sigma is a monotone transform of the softmax margin; the second axis is empty (temperature-invariance corollary)` and cross-check AUROC(fail) identity to machine precision. Policy-level mirror: `sigma_review_unique â 0` and fitted `s_max = inf`. **Never REJECT a degenerate Ï** â it is a *result* for the D2 vulnerability assessment, not an input error. The theorem is already in the repo (test 8); one slide, two theorems, same assert.

### 3.6 Fixtures + tests (2 h, same sitting as 3.3)

`src/make_sigma_fixture.py` (pure numpy, no torch): read an existing `runs/predictions/predictions_effb0_standard_eps2_255_test_*.npz`, append Ï from four generators â **oracle** (1âcorrect + noise; two-axis must beat one-axis on review rate at the same SLA â and the oracle row is itself a presentable number: the headroom a perfect uncertainty head would buy), **noisy-oracle**, **degenerate** (1âMSP; guard must fire, `s_max` must fit to inf), **mc-like** (oracle quantized to 21 levels â T=20 passes; exercises tie blocks) â with valid sidecars. Tests (`tests/test_sigma.py`, CPU like the existing 19): (a) `actions(sigma=None)` bit-identical; (b) 2-D fit includes +inf and never fits worse than 1-D on calib; (c) guard fires; (d) verify fails orphan Ï both directions; (e) AURC permutation-invariance on the coarse grid. Commit the generator, not the npz.

### 3.7 Demo + figure (week 2, 2 h + 1.5 h)

`src/demo.py`: `--uncertainty-npz` (repeatable) â `STATE['sigma_by_uid'] = dict(zip(z['uid'], z['sigma']))` after semantics check â the demo consumes files, never runs their model. Fix the uid plumbing: `/sample` currently strips the uid (`X-Name` sends `uid.split(':')[-1]`) â send the full uid in a new `X-Uid` header, client POSTs it back to `/analyze?uid=â¦`; uploads have no uid and display "sigma: not provided â one-axis policy (MSP)" (labeled fallback, never silent). `/analyze` gains Ï, `s_max`, the two-axis action, and a fourth rationale string. `/health` reports `s_max` + Ï source. `figures.py fig3`: scatter p_fake Ã Ï on test, points colored by true label, regions shaded by `actions(...)`, three threshold lines, clean beside one attacked condition â **the PIs' own decision diagram rendered with measured data**, the highest-leverage slide of the talk. Fixture until a real Ï lands; one command to regenerate.

**Total seam cost: ~13 h, zero GPU, zero network except the P5 push.** Everything from 3.3 onward is plane-capable.

---

## 4. PARALLEL vs GPU-CONTENDING AT CAMP

**His parallel lane (CPU, needs no GPU slot, runs while subgroups train):** refresh.py, verify hardening + tests, sigma policy/scoring/fixtures, compare.py fixes, README reorder (post-audit), brick packaging, runbook, talk arc â slides, the recording, scoring every incoming npz (Lane A is pure numpy). This is deliberately the bulk of his remaining work â the owner's camp position is *CPU-bound scorer beside a contended GPU*.

**GPU-contending (schedule into explicit slots, ~9 GB shared):** Day-1 ResNet parity block (â¤30 min â claim the 8 AM slot before subgroups do), resnet18 standard arm (~20 min, evening 1), scoring foreign *checkpoints* via the `d3_eval.sh` pattern (~10 min/model, Days 4â8 â this is Lane B and unavoidable GPU), optional eps sweep (20 min), teammates' late final runs before freeze. The demo needs no slot â CPU fallback is automatic (`src/demo.py:214-219`).

**Never his GPU, by the political boundary:** MC-Dropout passes, ensemble training, evidential heads, WP3's robust ResNet arms. Their forwards, their VRAM, their choices; his npz, his tables.

---

## 5. WHAT TO DECLINE

| Temptation | Why decline |
|---|---|
| Building any uncertainty estimator (MC-Dropout / ensembles / evidential), even "just a reference implementation" | The political constraint. It is the PIs' research area and three subgroups' assignment. The seam (Â§3) is designed so a fixture npz makes every argument an estimator would â score theirs, never build one. |
| Pre-building AT/TRADES ResNet arms | That is WP3's fortnight. Arrive with the frozen-probe floor + one standard arm proving the ckpt lane; offer the matched budgets only if asked. |
| AutoAttack on `vit_l14_e2e` | Square at ViT-L/14 throughput exceeds any budget; the proposal pre-authorizes "where feasible". One footnote in `d2_matrix.py` converts the blank cell into a signed scoping decision. |
| Three-class detection, localization/masks, OOD, extra backbones (incl. the B/16 bridge row unless tonight's bandwidth is truly free) | All explicitly "optional extensionsâ¦ only if time and compute allow" in the proposal Â§7. Binary is committed scope. |
| Settling the laundered-shortcut question (format/compression-matched subset) | Real science, named in `src/baselines.py`'s docstring â but it needs re-encoding + re-extraction + refits and serves no D1âD4 line. It is a *limitations slide line* and a future-work sentence, not camp work. |
| A second dataset (CIFAKE sits in the HF cache) | "A second dataset is a data adapter, not a rebuild" is proven by the ResNet adapter demo; actually running one dilutes the talk. |
| Refactoring `metrics.py` / the backbone plumbing / the stem naming scheme | All verified already-right; the stem fix is a 3-line guard, not a rename (a rename breaks every existing stem on disk). |
| Re-polishing D3, re-running published tables, duplicating the claim-audit | D3 is complete; the audit is running separately â touch README claims only after it merges. |
| Demo productization (auth, hosting, upload polish) | The demo's job is 3 minutes on 4 Sep. stdlib + localhost + recording fallback is the whole requirement. |
| Wide agent fleets / parallel infra for camp coordination | CONTRACT.md + submit checklist + refresh.py *is* the coordination layer. One file format beats any process. |

---

## 6. OWNER DECISIONS (options + recommendation; decisions are his)

**6.1 Licence for the brick.**
- MIT / BSD-3: maximum Factory reuse, zero reciprocity.
- **Apache-2.0 (recommended):** explicit patent grant, the default for research-consortium artifacts, the least-friction answer to "is this a TRAIL Factory brick". numpy/scikit-learn are BSD â no constraint either way.
- MPL-2.0: file-level copyleft if he wants harness improvements to stay open.
- AGPL-3.0: would deter the organizations the Factory exists to serve â anti-recommended.
- **Flag:** the PF grant's open-source bar may constrain anything shared with that dossier (due today) â check before signing a licence. No LICENSE file exists yet (verified).

**6.2 Repo visibility.** Private `github.com/samrent/trustfake`, 5 commits, history clean (notes/ and BUILD.md never committed â verified).
- (1) Invite collaborators as-is: 5 min, but exposes the "built pre-camp 19 Aug" framing and the pre-done WP2+WP3 story â a territorial insult to the WP3 subgroup.
- **(2) Recommended: new `trustfake-harness` repo** = CONTRACT.md + `src/{metrics,verify,score,compare,moderation,baselines,attack_suite}.py` + tests + manifest-sha sidecars + a team-facing README stripped of positioning. Create and push **tonight** (P6); it doubles as the brick seed. Hold D3 comparatives for staged reveal: "here is how a submission is scored" first, "here is what I measured" when the subgroup has its own numbers.
- (3) Contribute the same set as `harness/` subtree into the camp's Day-1 shared repo; private repo stays the lab notebook.

**6.3 ResNet-or-not.** **Recommend YES** to the cheap parity layer (weights tonight, frozen-probe rows + one standard arm Day 1, â¤1 h GPU total), **NO** to robust ResNet arms. Canonical tag decision needed *tonight* so the fetch list and Day-1 commands agree: **recommend `tv_in1k`** (torchvision parity â what the deck's "HuggingFace ResNet-18/50" almost certainly means), with `a1_in1k` fetched as insurance against camp code writing bare `resnet18`.

**6.4 Code-freeze date.** Unconfirmed Tue 1 Sep vs Thu 3 Sep. **Recommend: plan every code-bearing item to land by Mon 31 Aug** and treat 2â3 Sep as buffer that may not exist; the Sournac email line (P3) resolves it. The recording and refresh.py are the two items the pessimistic date squeezes â both are scheduled ahead of it above.

---

## 7. RISK TABLE â what kills the talk

| Risk | P | Mitigation in place | Mitigation needed |
|---|---|---|---|
| ResNet weights never fetched â camp baseline unloadable for two weeks | med (PF dossier eats tonight) | Verified absent; exact commands + acceptance test written | **P1 first, before the dossier. The only truly irreversible item.** |
| Tonight's GPU queue dies mid-run (clip_attacks crash, OOM on autoattack) | med | Per-arm nohup, `d2_matrix` renders `--` for missing cells, ViT column is bonus not spine | Check queue state before leaving; effb0 cells re-runnable at camp (small VRAM) |
| Sigma subgroup delivers late or never â two-axis story empty | med | Â§3.6 fixtures: the oracle-Ï headroom number and the degenerate-Ï theorem make the seam presentable with zero external input | Publish CONTRACT.md Day 1 so their format is ours from the start |
| A teammate's npz with shuffled/poisoned `y` silently corrupts D3/D4 tables | med | verify.py catches shape/dtype/uid-order today | **Week-1 ground-truth cross-check (verify hardening) â the one hole that produces confidently wrong slides** |
| Claim-audit collides with README edits | high if ignored | Audit runs separately; d2_matrix, CONTRACT.md, fig3 are new files (no collision) | Hard rule: no README claim/reorder edits until audit merges |
| Freeze is 1 Sep and week-2 items aren't done | med | Plan assumes 1 Sep; demo lane + fig3 are the only week-2 code, both â¤2 h | P3 email resolves the date; recording done pre-boarding (P9) |
| Live demo dies at the venue | low | stdlib, localhost, automatic CPU fallback, frozen policy loaded not computed | Runbook ladder + the P9 recording + screenshots |
| Political: WP3/uncertainty subgroups find their fortnight pre-done | med | notes/ + BUILD.md never committed | Harness-only repo (6.2 opt 2), staged reveal, floor-framing of the ResNet probe row, Ledda contradiction framed as "a question we want to put to you" |
| PF dossier consumes the entire pre-boarding window | high | Critical path pre-ranked; â core = ~3.5 attended hours; queue runs unattended | Accept: everything unstarred is plane/camp-capable by design |
| Presenting machine lacks assets (venv, caches, sample slice) | med | demo needs only CPU + 5 named paths | Runbook asset checklist; rsync the sample-slice of cache256, not the full 1.5 GB |
| `compare.py` crashes on the first foreign model id | high (verified `FileNotFoundError` path) | â | Day-1 graceful-skip fix, 1 h |
| Stem collision folds a tv-head onto an a1-backbone silently | low-med | Sidecars already record `model_id` | Day-1 3-line assert (guard, not rename) |

---

**The one-sentence version:** tonight buys weights, the sigma contract, and an unattended GPU queue; the plane writes the talk and the two-axis policy; Day 1 publishes the contract and the ResNet floor; week 1 turns the harness into the camp's scoreboard; week 2 only regenerates tables â and every claim on 4 Sep already has a file in `runs/` behind it.

*Key files cited: `README.md` (interface lines 10â25, results, limitations, gotchas), `notes/proposal_extracted.txt` (Table 1/2, D1âD4, "where feasible", Day-1/Days-6-8 language), the teambuilding deck (context panel, two-axis diagram, ResNet-18/50 panel), `src/{metrics,moderation,verify,score,compare,demo,figures,run_attacks,attack_suite,models,probe,features,train,baselines}.py`, `runs/{moderation_threatmodel,condition_table_threatmodel,d3_comparative,d4_*,trivial_baselines,moderation_report,clip_attacks.sh,train_wp3_eps8_255_collapsed.log}`, `tests/` (19 CPU tests, re-verifiable via `CUDA_VISIBLE_DEVICES="" .venv/bin/python -m pytest tests/ -q`). Verified live this session: 44 prediction npz + sidecars; `clip_attacks.sh` pgd_linf in flight, overconf queued; no resnet in any cache; no pip in the venv; no LICENSE/pyproject; private remote `samrent/trustfake`.*",
    "gaps": 38,
    "axes": [
      "The sigma seam â extending the npz contract, moderation policy, scoring, and demo so WP1/WP4 consume OTHER subgroups' per-image uncertainty (MC-Dropout / Deep Ensembles / evidential heads) without ever building it. Boundary rule: THEIRS = producing the sigma array (forward passes, ensemble members, scale choice, running it under attack conditions); OURS = the schema, validation, rank-based scoring, the fitted-and-frozen three-threshold policy, the degeneracy audit, and the demo display. The boundary is the npz file.",
      "Baseline parity with the deck (D1): ResNet-18/50 fixed backbone vs the built EffNet-B0 + CLIP L/14 â verdict: YES to a cheap ResNet parity row (weights pre-fetched, rows produced day-1 in <30 min GPU), NO to pre-building robust-trained ResNet arms. The deck names ResNet-18/50 as THE fixed backbone, so the camp WILL train ResNets; the highest-value position is arriving with (a) a frozen-probe ResNet row already in every table so no one can dismiss the harness as "not our baseline", and (b) the npz contract proven on a resnet stem, so WP1 scores THEIR fine-tuned/robust ResNets without touching their code. The frozen ResNet probe will likely score well below CLIP L/14 â that is the UniversalFakeDetect literature ordering (CLIP features > supervised-CNN features for fake detection), and it should be framed on day 1 as the floor that motivates the camp's fine-tuned ResNet, not as a weak result. Verified on disk: no models--timm--resnet* in ~/.cache/huggingface/hub, no ~/.cache/torch/hub/checkpoints at all â the LocalEntryNotFoundError claim is confirmed; every entrypoint (src/features.py --model, src/probe.py --model, src/score.py --model, src/models.py probe:<timm_id>/ckpt:<path>, src/train.py --backbone) accepts an arbitrary timm id with zero code changes.",
      "D2/D3 completeness against the deck's attack-defend list ("PGD Â· AutoAttack Â· uncertainty attacks Â· adv training Â· TRADES"): which {model x attack} cells exist in runs/predictions/, the minimal run-list to make D2 a complete deliverable, and how to present it as ONE vulnerability-assessment matrix",
      "The 4 Sep final talk and the TRAIL Factory brick â presentation arc from measured results, live-demo runbook for unreliable venue wifi, brick definition + packaging, README stranger-readability. Verified on disk 23 Aug: 19 CPU tests pass in 2.4s (CUDA_VISIBLE_DEVICES=""), all headline numbers cross-check across runs/condition_table_threatmodel.md, runs/moderation_threatmodel.md, runs/d3_comparative.md, runs/d4_*.md; no resnet anywhere in src/ or HF cache; no pyproject/LICENSE; demo cache256 = 1.5G at /home/samuel-renteria/Desktop/FILES/DATASETS/sid-set/cache256.",
      "Week-1 team integration: the Day-1 contract publication, stranger-proof submission and verification, the one-command matrix refresh, and the repo-sharing decision â for an owner landing with WP1+WP4 while three subgroups start WP2/WP3/uncertainty against him as scorer."
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Gap-map"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "gap:sigma-contract",
      "phaseIndex": 1,
      "phaseTitle": "Gap-map",
      "agentId": "afa396c24ad9b996c",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787515952427,
      "queuedAt": 1787515950436,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The sigma seam â extending the npz contract, moderation polâ¦",
      "promptPreview": "You are mapping a BUILT, MEASURED system against its FINAL DELIVERABLE and proposing pathways to
close the gap. Ground everything in files, not memory.

*** HARD CONSTRAINT: DO NOT USE THE GPU. *** A ViT-L/14 attack run occupies it. Do not import
torch onto cuda, do not create models, do not run src/{run_attacks,features,probe,train,demo,
attacks}. Reading source, runs/*.{md,json,log}, numpy over â¦",
      "lastProgressAt": 1787516261877,
      "tokens": 106551,
      "toolCalls": 13,
      "durationMs": 309449,
      "resultPreview": "{"axis":"The sigma seam â extending the npz contract, moderation policy, scoring, and demo so WP1/WP4 consume OTHER subgroups' per-image uncertainty (MC-Dropout / Deep Ensembles / evidential heads) without ever building it. Boundary rule: THEIRS = producing the sigma array (forward passes, ensemble members, scale choice, running it under attack conditions); OURS = the schema, validation, rank-baseâ¦"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "gap:baseline-parity",
      "phaseIndex": 1,
      "phaseTitle": "Gap-map",
      "agentId": "a74de3cf476f7b8ff",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787515952981,
      "queuedAt": 1787515950436,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Baseline parity with the deck (D1): ResNet-18/50 fixed backâ¦",
      "promptPreview": "You are mapping a BUILT, MEASURED system against its FINAL DELIVERABLE and proposing pathways to
close the gap. Ground everything in files, not memory.

*** HARD CONSTRAINT: DO NOT USE THE GPU. *** A ViT-L/14 attack run occupies it. Do not import
torch onto cuda, do not create models, do not run src/{run_attacks,features,probe,train,demo,
attacks}. Reading source, runs/*.{md,json,log}, numpy over â¦",
      "lastProgressAt": 1787516290055,
      "tokens": 97589,
      "toolCalls": 21,
      "durationMs": 335705,
      "resultPreview": "{"axis":"Baseline parity with the deck (D1): ResNet-18/50 fixed backbone vs the built EffNet-B0 + CLIP L/14 â verdict: YES to a cheap ResNet parity row (weights pre-fetched, rows produced day-1 in <30 min GPU), NO to pre-building robust-trained ResNet arms. The deck names ResNet-18/50 as THE fixed backbone, so the camp WILL train ResNets; the highest-value position is arriving with (a) a frozen-prâ¦"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "gap:d2-d3-completeness",
      "phaseIndex": 1,
      "phaseTitle": "Gap-map",
      "agentId": "a66156bfa11218141",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787515953226,
      "queuedAt": 1787515950437,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "D2/D3 completeness against the deck's attack-defend list ("â¦",
      "promptPreview": "You are mapping a BUILT, MEASURED system against its FINAL DELIVERABLE and proposing pathways to
close the gap. Ground everything in files, not memory.

*** HARD CONSTRAINT: DO NOT USE THE GPU. *** A ViT-L/14 attack run occupies it. Do not import
torch onto cuda, do not create models, do not run src/{run_attacks,features,probe,train,demo,
attacks}. Reading source, runs/*.{md,json,log}, numpy over â¦",
      "lastProgressAt": 1787516228981,
      "tokens": 99982,
      "toolCalls": 16,
      "durationMs": 274631,
      "resultPreview": "{"axis":"D2/D3 completeness against the deck's attack-defend list (\"PGD Â· AutoAttack Â· uncertainty attacks Â· adv training Â· TRADES\"): which {model x attack} cells exist in runs/predictions/, the minimal run-list to make D2 a complete deliverable, and how to present it as ONE vulnerability-assessment matrix","gaps":[{"gap":"The D2 'vulnerability assessment' exists only as scattered artifacts (conâ¦"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "gap:presentation",
      "phaseIndex": 1,
      "phaseTitle": "Gap-map",
      "agentId": "afdbc35633d25a8ac",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787515952772,
      "queuedAt": 1787515950437,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The 4 Sep final talk and the TRAIL Factory brick â presentaâ¦",
      "promptPreview": "You are mapping a BUILT, MEASURED system against its FINAL DELIVERABLE and proposing pathways to
close the gap. Ground everything in files, not memory.

*** HARD CONSTRAINT: DO NOT USE THE GPU. *** A ViT-L/14 attack run occupies it. Do not import
torch onto cuda, do not create models, do not run src/{run_attacks,features,probe,train,demo,
attacks}. Reading source, runs/*.{md,json,log}, numpy over â¦",
      "lastProgressAt": 1787516301984,
      "tokens": 98441,
      "toolCalls": 13,
      "durationMs": 347635,
      "resultPreview": "{"axis":"The 4 Sep final talk and the TRAIL Factory brick â presentation arc from measured results, live-demo runbook for unreliable venue wifi, brick definition + packaging, README stranger-readability. Verified on disk 23 Aug: 19 CPU tests pass in 2.4s (CUDA_VISIBLE_DEVICES=\"\"), all headline numbers cross-check across runs/condition_table_threatmodel.md, runs/moderation_threatmodel.md, runs/d3â¦"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "gap:team-integration",
      "phaseIndex": 1,
      "phaseTitle": "Gap-map",
      "agentId": "ab03de83d2ec8370c",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787515952958,
      "queuedAt": 1787515950437,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Week-1 team integration: the Day-1 contract publication, stâ¦",
      "promptPreview": "You are mapping a BUILT, MEASURED system against its FINAL DELIVERABLE and proposing pathways to
close the gap. Ground everything in files, not memory.

*** HARD CONSTRAINT: DO NOT USE THE GPU. *** A ViT-L/14 attack run occupies it. Do not import
torch onto cuda, do not create models, do not run src/{run_attacks,features,probe,train,demo,
attacks}. Reading source, runs/*.{md,json,log}, numpy over â¦",
      "lastProgressAt": 1787516245192,
      "tokens": 96733,
      "toolCalls": 17,
      "durationMs": 290842,
      "resultPreview": "{"already_strong":["The npz interface itself: README Â§'The interface â publish this on Day 1' is already contract-grade text (uid key, condition-from-line-one, sidecar fields). This is an extraction job, not a design job â do not redesign it.","verify.py already catches the biggest stranger-input classes: order-sensitive uid-sequence sha per split, partial-run n mismatch, y/logits dtype, duplicateâ¦"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesize:pathways",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "a87d75d89f2f4fe48",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787516305422,
      "queuedAt": 1787516303296,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n "def actions\|def fit_thresholds\|def evaluate_poliâ¦",
      "promptPreview": "You are mapping a BUILT, MEASURED system against its FINAL DELIVERABLE and proposing pathways to
close the gap. Ground everything in files, not memory.

*** HARD CONSTRAINT: DO NOT USE THE GPU. *** A ViT-L/14 attack run occupies it. Do not import
torch onto cuda, do not create models, do not run src/{run_attacks,features,probe,train,demo,
attacks}. Reading source, runs/*.{md,json,log}, numpy over â¦",
      "lastProgressAt": 1787516679717,
      "tokens": 108166,
      "toolCalls": 7,
      "durationMs": 374294,
      "resultPreview": "All anchors verified on disk. Writing the pathways document now.

---

# TRUSTFAKE WP1+WP4 â PATHWAYS DOCUMENT
## From the built stack to the 4 Sep deliverable
*Grounded 23 Aug 2026, ~22:20. All file paths under `/home/samuel-renteria/Desktop/FILES/PROJECTS/trustfake/wp1/` unless absolute. GPU status at writing: `runs/clip_attacks.sh` in flight (`vit_l14_e2e` pgd_linf running, overconf queued; 18.â¦"
    }
  ],
  "totalTokens": 607462,
  "totalToolCalls": 87
